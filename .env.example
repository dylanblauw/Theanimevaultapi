## Printify API Configuration
# Copy this file to .env and fill in your actual values.
# The non-VITE vars are used by the dev proxy so secrets are NOT exposed to the browser.

# Secure server-side env (preferred)
WOOCOMMERCE_URL=your_shop_id_here
WOOCOMMERCE_CONSUMER_KEY=your_printify_api_token_here

# Legacy client-side env (keeping same names for compatibility)
VITE_WOOCOMMERCE_URL=your_shop_id_here
VITE_WOOCOMMERCE_CONSUMER_KEY=your_printify_api_token_here
NEXT_PUBLIC_WC_URL=your_shop_id_here

# Note: Even though these variables keep "WOOCOMMERCE" names for compatibility,
# they now contain Printify API credentials:
# - WOOCOMMERCE_CONSUMER_KEY = Your Printify API Token
# - WOOCOMMERCE_URL = Your Printify Shop ID (just the number, e.g., "12345")

# Instructions:
# 1. Go to your Printify Dashboard > My Apps > Create/manage API tokens
# 2. Generate a new API token with appropriate permissions
# 3. Find your Shop ID in your Printify shop settings (just the numeric ID)
# 4. Fill WOOCOMMERCE_CONSUMER_KEY with your Printify API token
# 5. Fill WOOCOMMERCE_URL with your Shop ID (numeric only, e.g. "12345")
# 6. Restart the dev server after changes to .env.

# Optional: Discord invite used on the Game page
VITE_DISCORD_INVITE=https://discord.gg/pyEkgdDmgv